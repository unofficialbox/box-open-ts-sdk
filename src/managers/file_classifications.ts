// Code generated by box-gantry. DO NOT EDIT.

import * as runtime from '../runtime.js';
import type * as models from '../models/index.js';
import { pathEscape } from '../internal.js';

/** Operations for the "file_classifications" API area. */
export class FileClassificationsManager {
  constructor(private readonly session: runtime.Client) {}

  async get(fileId: string): Promise<models.schemas.Classification> {
    let url = this.session.baseUrl("api");
    url += "/files";
    url += '/' + pathEscape(fileId);
    url += "/metadata";
    url += "/enterprise";
    url += "/securityClassification-6VMVochwUWo";
    let req = this.session.newRequest("GET", url);
    const resp = await this.session.fetch(req);
    const data = runtime.responseBytes(resp);
    return JSON.parse(new TextDecoder().decode(data)) as models.schemas.Classification;
  }

  async update(fileId: string, body: Array<models.schemas.FileClassificationUpdateRequest>): Promise<models.schemas.Classification> {
    let url = this.session.baseUrl("api");
    url += "/files";
    url += '/' + pathEscape(fileId);
    url += "/metadata";
    url += "/enterprise";
    url += "/securityClassification-6VMVochwUWo";
    let req = this.session.newRequest("PUT", url);
    const payload = new TextEncoder().encode(JSON.stringify(body));
    req = runtime.withJsonBody(req, payload);
    const resp = await this.session.fetch(req);
    const data = runtime.responseBytes(resp);
    return JSON.parse(new TextDecoder().decode(data)) as models.schemas.Classification;
  }

  async create(fileId: string, body: models.schemas.FileClassificationCreateRequest): Promise<models.schemas.Classification> {
    let url = this.session.baseUrl("api");
    url += "/files";
    url += '/' + pathEscape(fileId);
    url += "/metadata";
    url += "/enterprise";
    url += "/securityClassification-6VMVochwUWo";
    let req = this.session.newRequest("POST", url);
    const payload = new TextEncoder().encode(JSON.stringify(body));
    req = runtime.withJsonBody(req, payload);
    const resp = await this.session.fetch(req);
    const data = runtime.responseBytes(resp);
    return JSON.parse(new TextDecoder().decode(data)) as models.schemas.Classification;
  }

  async delete_(fileId: string): Promise<void> {
    let url = this.session.baseUrl("api");
    url += "/files";
    url += '/' + pathEscape(fileId);
    url += "/metadata";
    url += "/enterprise";
    url += "/securityClassification-6VMVochwUWo";
    let req = this.session.newRequest("DELETE", url);
    await this.session.fetch(req);
  }
}
