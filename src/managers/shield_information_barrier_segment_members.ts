// Code generated by box-gantry. DO NOT EDIT.

import * as runtime from '../runtime.js';
import type * as models from '../models/index.js';
import { pathEscape } from '../internal.js';

/** Optional parameters for ShieldInformationBarrierSegmentMembersManager.list. */
export interface ShieldInformationBarrierSegmentMembersManagerListOptions {
  marker?: string;
  limit?: number;
}

/** Operations for the "shield_information_barrier_segment_members" API area. */
export class ShieldInformationBarrierSegmentMembersManager {
  constructor(private readonly session: runtime.Client) {}

  async get(shieldInformationBarrierSegmentMemberId: string): Promise<models.schemas.ShieldInformationBarrierSegmentMember> {
    let url = this.session.baseUrl("api");
    url += "/shield_information_barrier_segment_members";
    url += '/' + pathEscape(shieldInformationBarrierSegmentMemberId);
    let req = this.session.newRequest("GET", url);
    const resp = await this.session.fetch(req);
    const data = runtime.responseBytes(resp);
    return JSON.parse(new TextDecoder().decode(data)) as models.schemas.ShieldInformationBarrierSegmentMember;
  }

  async delete_(shieldInformationBarrierSegmentMemberId: string): Promise<void> {
    let url = this.session.baseUrl("api");
    url += "/shield_information_barrier_segment_members";
    url += '/' + pathEscape(shieldInformationBarrierSegmentMemberId);
    let req = this.session.newRequest("DELETE", url);
    await this.session.fetch(req);
  }

  /** Iterate every `models.schemas.ShieldInformationBarrierSegmentMember` across pages, threading the marker cursor. */
  async *list(shieldInformationBarrierSegmentId: string, opts?: ShieldInformationBarrierSegmentMembersManagerListOptions): AsyncIterableIterator<models.schemas.ShieldInformationBarrierSegmentMember> {
    const options: ShieldInformationBarrierSegmentMembersManagerListOptions = { ...opts };
    for (;;) {
      const page = await this.listPage(shieldInformationBarrierSegmentId, options);
      const items = page.entries ?? [];
      for (const item of items) {
        yield item;
      }
      const cursor = page.next_marker;
      if (cursor === undefined || cursor === null || cursor === '') {
        return;
      }
      options.marker = cursor;
    }
  }

  private async listPage(shieldInformationBarrierSegmentId: string, opts?: ShieldInformationBarrierSegmentMembersManagerListOptions): Promise<models.schemas.ShieldInformationBarrierSegmentMembers> {
    let url = this.session.baseUrl("api");
    url += "/shield_information_barrier_segment_members";
    let req = this.session.newRequest("GET", url);
    req = runtime.withQuery(req, "shield_information_barrier_segment_id", shieldInformationBarrierSegmentId);
    const options = opts ?? {};
    if (options.marker !== undefined) {
      req = runtime.withQuery(req, "marker", options.marker);
    }
    if (options.limit !== undefined) {
      req = runtime.withQuery(req, "limit", String(options.limit));
    }
    const resp = await this.session.fetch(req);
    const data = runtime.responseBytes(resp);
    return JSON.parse(new TextDecoder().decode(data)) as models.schemas.ShieldInformationBarrierSegmentMembers;
  }

  async create(body: models.schemas.ShieldInformationBarrierSegmentMemberCreateRequest): Promise<models.schemas.ShieldInformationBarrierSegmentMember> {
    let url = this.session.baseUrl("api");
    url += "/shield_information_barrier_segment_members";
    let req = this.session.newRequest("POST", url);
    const payload = new TextEncoder().encode(JSON.stringify(body));
    req = runtime.withJsonBody(req, payload);
    const resp = await this.session.fetch(req);
    const data = runtime.responseBytes(resp);
    return JSON.parse(new TextDecoder().decode(data)) as models.schemas.ShieldInformationBarrierSegmentMember;
  }
}
