// Code generated by box-gantry. DO NOT EDIT.

import * as runtime from '../runtime.js';
import type * as models from '../models/index.js';
import { pathEscape } from '../internal.js';

/** Operations for the "skills" API area. */
export class SkillsManager {
  constructor(private readonly session: runtime.Client) {}

  async get(fileId: string): Promise<models.schemas.SkillCardsMetadata> {
    let url = this.session.baseUrl("api");
    url += "/files";
    url += '/' + pathEscape(fileId);
    url += "/metadata";
    url += "/global";
    url += "/boxSkillsCards";
    let req = this.session.newRequest("GET", url);
    const resp = await this.session.fetch(req);
    const data = runtime.responseBytes(resp);
    return JSON.parse(new TextDecoder().decode(data)) as models.schemas.SkillCardsMetadata;
  }

  async update(fileId: string, body: Array<models.schemas.SkillUpdateRequest>): Promise<models.schemas.SkillCardsMetadata> {
    let url = this.session.baseUrl("api");
    url += "/files";
    url += '/' + pathEscape(fileId);
    url += "/metadata";
    url += "/global";
    url += "/boxSkillsCards";
    let req = this.session.newRequest("PUT", url);
    const payload = new TextEncoder().encode(JSON.stringify(body));
    req = runtime.withJsonBody(req, payload);
    const resp = await this.session.fetch(req);
    const data = runtime.responseBytes(resp);
    return JSON.parse(new TextDecoder().decode(data)) as models.schemas.SkillCardsMetadata;
  }

  async create(fileId: string, body: models.schemas.SkillCreateRequest): Promise<models.schemas.SkillCardsMetadata> {
    let url = this.session.baseUrl("api");
    url += "/files";
    url += '/' + pathEscape(fileId);
    url += "/metadata";
    url += "/global";
    url += "/boxSkillsCards";
    let req = this.session.newRequest("POST", url);
    const payload = new TextEncoder().encode(JSON.stringify(body));
    req = runtime.withJsonBody(req, payload);
    const resp = await this.session.fetch(req);
    const data = runtime.responseBytes(resp);
    return JSON.parse(new TextDecoder().decode(data)) as models.schemas.SkillCardsMetadata;
  }

  async delete_(fileId: string): Promise<void> {
    let url = this.session.baseUrl("api");
    url += "/files";
    url += '/' + pathEscape(fileId);
    url += "/metadata";
    url += "/global";
    url += "/boxSkillsCards";
    let req = this.session.newRequest("DELETE", url);
    await this.session.fetch(req);
  }

  async updateSkillInvocation(skillId: string, body: models.schemas.SkillInvocationUpdateRequest): Promise<void> {
    let url = this.session.baseUrl("api");
    url += "/skill_invocations";
    url += '/' + pathEscape(skillId);
    let req = this.session.newRequest("PUT", url);
    const payload = new TextEncoder().encode(JSON.stringify(body));
    req = runtime.withJsonBody(req, payload);
    await this.session.fetch(req);
  }
}
